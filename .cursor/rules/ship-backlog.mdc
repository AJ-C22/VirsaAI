---
description: Open ship backlog — consult when user asks what's next
alwaysApply: true
---

# VirsaAI — open todos

When the user asks what to do next, what’s left, or similar, consult this list and `SHIP.md`.

## Still to do (owner action / ship)

1. **Configure Stripe (keys not set yet)** — Checkout code is live, but env is incomplete until:
   - Create Family ($19) + Legacy ($49) recurring Prices in Stripe
   - Set `STRIPE_SECRET_KEY`, `STRIPE_PRICE_FAMILY`, `STRIPE_PRICE_LEGACY`, `STRIPE_WEBHOOK_SECRET`, `APP_URL` in `.env`
   - Run `stripe listen --forward-to localhost:8000/billing/webhook` (local) or point Dashboard webhooks at `POST /billing/webhook`
   - Confirm `/plans` returns `"stripe_configured": true`
2. Set a strong `VIRSA_JWT_SECRET`
3. Host Postgres (Supabase) + apply schemas / RLS
4. Move audio/artifacts to Supabase Storage
5. Deploy API worker (Whisper) + Next.js (`NEXT_PUBLIC_API_ROOT`)
6. Enforce vault membership on every write (not optional Bearer)

## Nice-to-have

- Email delivery for invites (copy-link works today)
- Embedding-based shared-memory matching
- Full heirloom book beyond print/PDF
- Mobile recording PWA

## Already done (don’t re-suggest as next unless asked)

- Product UI polish, invite accept UI, shared-memory unlink, Stripe Checkout **code**
- Signup → record → dashboard pipeline
